This directory is reserved for MANDO-related modules.

The current integration uses `models/MANDO_Net.py` as an in-repo
lightweight graph encoder/classifier compatible with existing
federated training interfaces.
